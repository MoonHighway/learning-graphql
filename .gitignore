node_modules

# Environment variables
# ##
# TODO: Uncomment these once you have added your secrets/environment variables and then manually remove them from git tracking with:
#   $ git rm --cached nextjs-with-apollo/.env
#   $ git rm --cached photo-share-api/.env
#   $ git rm --cached photo-share-client/.env
# ##
# nextjs-with-apollo/.env
# photo-share-api/.env
# photo-share-client/.env

# Modules should be installed on Docker containers; ignore local package-lock.json files
package-lock.json
nextjs-with-apollo/package-lock.json
photo-share-api/package-lock.json
photo-share-client/package-lock.json

# Docker and related assets
photo-share-api/.docker
photo-share-api/assets

# Build
nextjs-with-apollo/.next

# Presentations
__presentations/node_modules
__presentations/dist
